<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Translucent overlay theme so CameraActivity looks like a small in-app
        scanner box floating over the current screen (dimmed behind it),
        instead of a fullscreen native camera app.
    -->
    <style name="Theme.Scanner.Translucent" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
    </style>
</resources>
